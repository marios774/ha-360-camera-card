# Presets

Permanent presets are stored in the Lovelace card configuration under `presets`. Temporary presets created from the live card are stored in browser local storage under `<storage_key>:temp-presets` and display a clock badge. Open the dashboard card editor to import a temporary preset into YAML. A maximum of four permanent and temporary presets is shown.
