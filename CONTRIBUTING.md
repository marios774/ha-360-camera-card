# Contributing

1. Fork the repository.
2. Create a feature branch.
3. Run `npm ci`.
4. Run `npm run build`.
5. Run `npm test`.
6. Commit source and the generated `dist/ha-360-camera-card.js`.
7. Open a pull request.

Do not include camera credentials, stream tokens, private addresses or images
of private areas in issues or pull requests.
